{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/databricks-solutions/lakebase-app-dev-kit/scripts/lakebase/scm-workflow-state.schema.json",
  "title": "SCM Workflow State",
  "description": "Gate surface for the lakebase-scm-workflows state machine. Lives at .lakebase/workflow-state.json in a paired project. One file per project root (single feature in flight at a time, v1).",
  "type": "object",
  "required": ["version", "state", "tier_topology", "project_id"],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional schema reference for editor tooling."
    },
    "version": {
      "type": "integer",
      "const": 1,
      "description": "Schema version. Bumped when the shape changes incompatibly."
    },
    "state": {
      "type": "string",
      "enum": [
        "scaffold-complete",
        "feature-claimed",
        "pr-ready",
        "ci-green",
        "merged"
      ],
      "description": "Current state in the SCM workflow."
    },
    "tier_topology": {
      "type": "integer",
      "enum": [1, 2, 3],
      "description": "Long-running tier count chosen at scaffold. 1=prod, 2=prod+staging, 3=prod+staging+dev."
    },
    "project_id": {
      "type": "string",
      "minLength": 1,
      "description": "LAKEBASE_PROJECT_ID, mirrored from .env so the state file is self-contained."
    },
    "feature_id": {
      "type": "string",
      "minLength": 1,
      "description": "Feature identifier (e.g. F1-initial-domain). Required from feature-claimed onward."
    },
    "branch": {
      "type": "string",
      "minLength": 1,
      "description": "Git/Lakebase branch name for the current feature. Required from feature-claimed onward."
    },
    "parent_branch": {
      "type": "string",
      "minLength": 1,
      "description": "Lakebase parent the feature branch forks from (production for tier 1, staging for tier 2, dev for tier 3). Required from feature-claimed onward."
    },
    "lakebase_branch_uid": {
      "type": "string",
      "minLength": 1,
      "description": "Lakebase-assigned branch UID (e.g. br-broad-sky-d2k5gewt). Required from feature-claimed onward."
    },
    "claimed_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 timestamp of feature-claim. Required from feature-claimed onward."
    },
    "pr_url": {
      "type": "string",
      "format": "uri",
      "description": "GitHub PR URL. Required from pr-ready onward."
    },
    "pushed_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 timestamp of git push. Required from pr-ready onward."
    },
    "ci_run_url": {
      "type": "string",
      "format": "uri",
      "description": "GitHub Actions / CI run URL. Required from ci-green onward."
    },
    "ci_green_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 timestamp when CI turned green. Required from ci-green onward."
    },
    "merged_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 timestamp of merge. Required from merged state."
    },
    "migrate_run_url": {
      "type": "string",
      "format": "uri",
      "description": "GitHub Actions URL for the downstream migrate workflow that applied migrations to the parent branch's Lakebase pair. Optional on merged: populated only when scm-merge ran with --wait-migrate (default)."
    },
    "migrate_completed_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO-8601 timestamp when the downstream migrate workflow finished. Paired with migrate_run_url."
    }
  },
  "allOf": [
    {
      "if": { "properties": { "state": { "const": "feature-claimed" } } },
      "then": {
        "required": ["feature_id", "branch", "parent_branch", "lakebase_branch_uid", "claimed_at"]
      }
    },
    {
      "if": { "properties": { "state": { "const": "pr-ready" } } },
      "then": {
        "required": [
          "feature_id",
          "branch",
          "parent_branch",
          "lakebase_branch_uid",
          "claimed_at",
          "pr_url",
          "pushed_at"
        ]
      }
    },
    {
      "if": { "properties": { "state": { "const": "ci-green" } } },
      "then": {
        "required": [
          "feature_id",
          "branch",
          "parent_branch",
          "lakebase_branch_uid",
          "claimed_at",
          "pr_url",
          "pushed_at",
          "ci_run_url",
          "ci_green_at"
        ]
      }
    },
    {
      "if": { "properties": { "state": { "const": "merged" } } },
      "then": {
        "required": [
          "feature_id",
          "branch",
          "parent_branch",
          "lakebase_branch_uid",
          "claimed_at",
          "pr_url",
          "pushed_at",
          "ci_run_url",
          "ci_green_at",
          "merged_at"
        ]
      }
    }
  ],
  "additionalProperties": false
}
